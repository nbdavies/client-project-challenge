<% if @versions %>
  <ul>
  <% @versions.each do |version| %>
    <li>
      <%= link_to version.article.title, article_version_path(@article.id, version.id) %>
      <%= version.author.username %> - <%= time_ago_in_words(version.created_at) %> ago
    </li>
  <% end %>
  </ul>
<% end %>

